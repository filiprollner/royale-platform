{
  "buildCommand": "cd ../../ && npm run build --workspace=packages/shared && npm run build --workspace=packages/ui && cd apps/web && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
